{
  "name": "screeps",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:raghudevan/screeps.git",
  "author": "Raghudevan Shankar <raghudevan@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "watch:deploy": "nodemon --watch src --ext ts --exec \"yarn build:deploy\"",
    "dev": "nodemon --watch src --ext ts --exec \"tsc\"",
    "clean": "rimraf dist",
    "deploy": "node scripts/deploy.js",
    "build:deploy": "yarn build && yarn deploy"
  },
  "devDependencies": {
    "@types/node": "^24.0.13",
    "copyfiles": "^2.4.1",
    "nodemon": "^3.1.10",
    "rimraf": "5.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "dotenv": "^17.2.0"
  }
}
